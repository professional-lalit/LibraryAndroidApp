<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.library.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".screens.main.BookDetailsActivity"></activity>
        <activity android:name=".screens.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.onboarding.forgot_password.ForgotPasswordActivity" />
        <activity android:name=".screens.onboarding.change_password.ChangePasswordActivity" />
        <activity android:name=".screens.onboarding.signup.SignupActivity" />
        <activity android:name=".screens.onboarding.login.LoginActivity" />
        <activity android:name=".screens.main.MainActivity" />
    </application>

</manifest>